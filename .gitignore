# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/
.venv/
.pytest_cache/

# OS / editor
.DS_Store
.vscode/
.idea/

# Logs / runtime artifacts
*.log

# Secrets / local config — DB passwords. Nothing else should be excluded
# from git: per-project policy is "everything except API keys, usernames,
# and passwords gets pushed to github."
config/postgres.yaml
config/qsa.yaml.local
